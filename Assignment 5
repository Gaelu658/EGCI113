#include <stdio.h>

int main()
{
    int x,i,count;
    printf("Input a number:");
    scanf("%d",&x);
    count=0;
    
    while(x >0){
        if( x%10==3) count++;
        x/=10;
        
    }
    
    printf("There are %d 3's in this number\n",count);
    
    return 0;  
    
}
